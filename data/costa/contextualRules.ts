import type {
  ContextualPackageRule,
} from "@/lib/contextualPackageRules";

/*
 * REGLAS CONTEXTUALES COSTA
 *
 * Costa participa en el mismo sistema
 * contextual que el resto de navieras.
 *
 * Este proveedor permanece vacío hasta
 * disponer de reglas comerciales Costa
 * respaldadas por evidencia suficiente.
 */
export const costaContextualPackageRules:
  ContextualPackageRule[] = [];
